// const students = await Course.findOne({ _id: courseObjId }, { _id: 1 }).populate("students");
// data.dueDate = new Date(data.dueDate);
// await Student.findOneAndUpdate({ email }, student, { new: true });
// course.students.push(studObjId);

// why unique true not working for customer
